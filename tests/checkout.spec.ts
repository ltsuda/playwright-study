import { expect, test } from '../src/fixtures';

test.describe('Checkout Suite: @checkout', () => {
  test('should be at checkout page url', async () => {
    expect(true).toBeFalsy();
  });
  test("should be have information field's placeholder", async () => {
    expect(true).toBeFalsy();
  });
  test('should show first name error is required', async () => {
    expect(true).toBeFalsy();
  });
  test('should show last name error is required', async () => {
    expect(true).toBeFalsy();
  });
  test('should show postal code error is required', async () => {
    expect(true).toBeFalsy();
  });
  test('should be at cart page if cancel checkout', async () => {
    expect(true).toBeFalsy();
  });
});
