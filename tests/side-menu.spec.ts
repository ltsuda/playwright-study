import { expect, test } from '../src/fixtures';

test.describe('Side Menu Suite: @side-menu', () => {
  test('should open side menu', async () => {
    expect(true).toBeFalsy();
  });
  test('should close side menu', async () => {
    expect(true).toBeFalsy();
  });
  test('should navigate back to inventory list page', async () => {
    expect(true).toBeFalsy();
  });
  test('should have saucelabs link in about menu item', async () => {
    expect(true).toBeFalsy();
  });
  test('should logout from application', async () => {
    expect(true).toBeFalsy();
  });
  test('should reset cart badge', async () => {
    expect(true).toBeFalsy();
  });
});
